# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# Since ZMK moved to Zephyr 4.1, board IDs need the `zmk` variant
# qualifier and an @revision instead of a version suffix, so the board
# formerly known as nice_nano_v2 is now nice_nano@2.0.0//zmk. See
# https://zmk.dev/blog/2025/12/09/zephyr-4-1#zmk-board-variant
#
# board: [ "nice_nano@2.0.0//zmk" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: nice_nano@1.0.0//zmk
#   - board: nice_nano@2.0.0//zmk
#     shield: reviung41
#
---
include:
  - board: nice_nano@2.0.0//zmk
    shield: tbkmini_left
  - board: nice_nano@2.0.0//zmk
    shield: tbkmini_right
  - board: nice_nano@2.0.0//zmk
    shield: settings_reset
